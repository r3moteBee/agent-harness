# File Organizer

A simple utility that organizes files in the user's workspace by type.

1. Ask the user which directory to organize
2. Run the organizer script to sort files
3. Report back what was moved

This skill only reads files and renames them — nothing leaves the machine.
